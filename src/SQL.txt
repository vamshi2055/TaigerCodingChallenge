SELECT 
    DEPARTMENT.NAME, 
    COUNT(EMPLOYEE.ID)
FROM 
    DEPARTMENT
LEFT JOIN 
    EMPLOYEE ON DEPARTMENT.DEPT_ID = EMPLOYEE.DEPT_ID
GROUP BY 
    DEPARTMENT.DEPT_ID, 
    DEPARTMENT.NAME
ORDER BY 
    COUNT(EMPLOYEE.ID) DESC, 
    DEPARTMENT.NAME;